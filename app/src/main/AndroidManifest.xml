<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoveOn">

        <!-- 1. 카카오 앱 키 설정 (실제 네이티브 앱 키 입력) -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="25c2aa0be664ddb3d7c4f966ba38db69" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.LoveOn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. ★ 카카오톡 로그인 리다이렉트를 위한 필수 액티비티 추가 ★ -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- ⚠️ 카카오톡 로그인 규칙: kakao 뒤에 공백 없이 실제 네이티브 앱 키를 붙입니다. -->
                <!-- 예: 네이티브 키가 12345라면 android:scheme="kakao12345" -->
                <data android:host="oauth"
                    android:scheme="kakao25c2aa0be664ddb3d7c4f966ba38db69" />
            </intent-filter>
        </activity>

    </application>
</manifest>